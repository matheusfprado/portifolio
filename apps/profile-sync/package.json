{
  "name": "@portfolio/profile-sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node --env-file-if-exists=../../.env.local dist/server.js",
    "sync": "npm run build && node --env-file-if-exists=../../.env.local dist/job.js"
  },
  "dependencies": {
    "@portfolio/profile": "*"
  }
}
